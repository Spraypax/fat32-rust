# fat32-rust

Mini lecteur FAT32 en Rust (lib `no_std` + `alloc`) avec un petit CLI de test.

## Prérequis
- Rust toolchain (stable)
- `mkfs.vfat` (paquet `dosfstools`)

## Build
```bash
cargo build
